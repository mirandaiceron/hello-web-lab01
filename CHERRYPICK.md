# Cherry-Pick

`dd98836` on `lab09-feature` and `63920f0` on `lab09-hotfix` contain the exact same change, but are two different commits with two different hashes. `git cherry-pick` copies a commit, it does not move it.